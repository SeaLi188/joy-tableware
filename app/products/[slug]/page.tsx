import type {Metadata} from "next";import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {getProduct,products} from "@/data/products";import {InquiryCTA} from "@/components/ui";
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const p=getProduct((await params).slug);return {title:p?.name||"Product",description:p?.description}}
export default async function Product({params}:{params:Promise<{slug:string}>}){const p=getProduct((await params).slug);if(!p)notFound();return <><section className="container-site grid gap-12 pb-24 pt-36 lg:grid-cols-[1.1fr_.9fr] lg:gap-20"><div className="relative aspect-square overflow-hidden bg-sand"><Image src={p.image} alt={p.name} fill priority sizes="(max-width:1024px) 100vw, 55vw" className="object-cover"/></div><div className="self-center"><p className="eyebrow">{p.category}</p><h1 className="display mt-5 text-6xl md:text-7xl">{p.name}</h1><p className="mt-6 leading-7 text-muted">{p.description}</p><dl className="mt-9 divide-y border-y text-sm">{[["Material",p.material],["Available Sizes",p.sizes.join(" · ")],["Available Colors",p.colors.join(" · ")],["Applications",p.applications.join(" · ")],["Customization",p.customization.join(" · ")],["MOQ",p.moq],["Packaging",p.packaging]].map(([k,v])=><div className="grid grid-cols-[130px_1fr] gap-5 py-4" key={k}><dt className="text-muted">{k}</dt><dd>{v}</dd></div>)}</dl><div className="mt-8 flex flex-wrap gap-3"><Link href="/contact" className="btn btn-dark">Request a Quote</Link><Link href="/contact" className="btn btn-outline">Contact Us</Link><a target="_blank" href="https://cnjoytableware88.m.en.alibaba.com/" className="btn btn-outline">View on Alibaba</a></div></div></section><InquiryCTA/></>}
