import Image from "next/image";import Link from "next/link";import {ArrowUpRight} from "lucide-react";import type {Product} from "@/data/products";
export function SectionTitle({eyebrow,title,text,center=false}:{eyebrow?:string;title:string;text?:string;center?:boolean}){return <div className={`mb-12 max-w-2xl ${center?"mx-auto text-center":""}`}>{eyebrow&&<p className="eyebrow mb-4">{eyebrow}</p>}<h2 className="display text-5xl md:text-6xl">{title}</h2>{text&&<p className="mt-5 leading-7 text-muted">{text}</p>}</div>}
export function ProductCard({product}:{product:Product}){return <Link href={`/products/${product.slug}`} className="image-zoom group block"><div className="relative aspect-[4/5] overflow-hidden bg-sand"><Image src={product.image} alt={product.name} fill sizes="(max-width:768px) 100vw, 33vw" className="object-cover"/><span className="absolute bottom-4 right-4 flex h-10 w-10 translate-y-2 items-center justify-center rounded-full bg-white opacity-0 transition group-hover:translate-y-0 group-hover:opacity-100"><ArrowUpRight size={16}/></span></div><p className="eyebrow mt-5">{product.category}</p><h3 className="mt-2 font-serif text-2xl">{product.name}</h3><p className="mt-1 text-sm text-muted">{product.subtitle}</p></Link>}
export function PageHero({eyebrow,title,text}:{eyebrow:string;title:string;text:string}){return <section className="bg-ivory pb-20 pt-40 text-center"><div className="container-site"><p className="eyebrow mb-5">{eyebrow}</p><h1 className="display mx-auto max-w-4xl text-6xl md:text-8xl">{title}</h1><p className="mx-auto mt-6 max-w-2xl leading-7 text-muted">{text}</p></div></section>}
export function InquiryCTA(){return <section className="bg-sand py-24 text-center"><div className="container-site"><p className="eyebrow mb-4">Start a conversation</p><h2 className="display text-5xl md:text-6xl">Looking for Tableware for<br className="hidden md:block"/> Your Next Collection?</h2><p className="mx-auto mt-6 max-w-2xl text-muted">Tell us what you are looking for. Our team will help with product selection, customization and wholesale quotations.</p><div className="mt-9 flex flex-wrap justify-center gap-3"><Link className="btn btn-dark" href="/contact">Request a Quote</Link><Link className="btn btn-outline" href="/contact">Contact Us</Link></div></div></section>}
