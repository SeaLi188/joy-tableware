"use client";import Link from "next/link";import {useState} from "react";import {Menu,X} from "lucide-react";
const links=[["Products","/products"],["Collections","/collections"],["Customization","/customization"],["About Us","/about"],["Contact","/contact"]];
export default function Header(){const [open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 border-b border-black/10 bg-white/95 backdrop-blur"><div className="container-site flex h-20 items-center justify-between"><Link href="/" className="font-serif text-xl tracking-[.14em]">JOY <span className="text-gold">TABLEWARE</span></Link><nav className="hidden items-center gap-8 lg:flex">{links.map(([l,h])=><Link key={h} href={h} className="text-xs tracking-[.08em] hover:text-gold">{l}</Link>)}<Link href="/contact" className="btn btn-dark">Request a Quote</Link></nav><button className="lg:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t bg-white px-6 py-6 lg:hidden">{links.map(([l,h])=><Link key={h} href={h} onClick={()=>setOpen(false)} className="block border-b py-4 text-sm">{l}</Link>)}<Link href="/contact" onClick={()=>setOpen(false)} className="btn btn-dark mt-5 w-full">Request a Quote</Link></nav>}</header>}
